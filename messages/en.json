import {defineRouting} from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['en', 'th'],
  defaultLocale: 'en',
  // Optional: hide default locale in URL (e.g., /dashboard instead of /en/dashboard)
  localePrefix: 'as-needed'
});